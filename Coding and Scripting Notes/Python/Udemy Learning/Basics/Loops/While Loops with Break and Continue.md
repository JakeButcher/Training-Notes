If we want to use break and continue, we can do the same as before.

If the user inputs the correct username "pypy" then the loop will stop, otherwise continue will rerun the code from line 1:

![alt text](image-7.png)